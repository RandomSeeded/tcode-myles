the nanocode format is specified in terms of byte strings.
this implementation only supports the subset of byte strings that are js strings.

todo:
- encode and decode in own files
- both exported from index.js
- better name
  - zencode?
